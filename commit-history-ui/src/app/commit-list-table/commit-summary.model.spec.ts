import { CommitSummaryModel } from './commit-summary.model';

describe('CommitSummaryModel', () => {
  it('should create an instance', () => {
    expect(new CommitSummaryModel()).toBeTruthy();
  });
});
